import { filters } from './_constants'

const buttons = [
  { id: filters.filter_all, title: 'All' },
  { id: filters.filter_active, title: 'Active' },
  { id: filters.filter_completed, title: 'Completed' }
]

export default buttons
